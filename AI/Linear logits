###回归：
  通过你的预测最终获得一个值，这样的叫做回归
###分类：
  最终得到的是一个分类结果，而不是值
###例如：银行贷款问题
  多数据可有多个特征
  输入输出到关系：工资/年龄对最终结果产生到影响
  通过工资/年龄两个特征获得一条好的拟合曲线
  设置年龄为xita1,工资为xita2
  拟合曲线或者平面为：h=xita0+x1×xita1+x2×xita2
    xita0为偏置项，额度可以上下浮动，影响额度较小
    xita1,xita2为权重项，影响额度较大
    x1,x2为年龄和工资到数据
    h为额度
    x0=1
###误差：
  真实值和预测值之间存在误差
  真实值=预测值（拟合曲线或者平面）+误差值
  误差：独立且具有相同分布，服从均值为0方差为xita平反到高斯分布
  ###贷款问题：
    每个人借款：相互独立
    都来假定到银行：相同分布
    银行多借少借浮动大多数情况下浮动不大：高斯分布
    高斯-1～1之间到面积：为绝大多数情况到额度值，偏差小为大多数额度情况
    ps：只要模型得出到结果符合基本规则，预测的模型就是可用的，选择模型是假设的过程
  ###高斯分布表达式：xxxx
    误差值为e，改下高斯分布，带入原式
    方差或者叫标准差为xigama
  ###似然函数：
    根据样本估计参数值
    赌场10个人里9个人都挣钱了，我觉得我也能挣钱
    ###极大似然：
      预测值距离真实值的概率越大越好
    ###对数似然：
      ###优点：可以把累乘变成累加
      log （a×b）= log a + log b
      ###最小二分法：各个{1/2*[（真实值-预测值）^2]}求和
          矩阵中到平方=矩阵转置×矩阵本身
          要让xita为最小值点《-----偏导等于0的点
          xita可在x，y已知情况下，可求解
      ###总结：对数似然化简以后，发现可以使用最小二分法，让最小二分法到值越小，误差越小
              ，对应到则是需要真实值和预测值越接近越好
###评估方法：
    R^2评估项 1-？  ---》R^2越等于1越好
              
  
  
  
